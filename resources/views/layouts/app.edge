<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    @entryPointStyles('app')
    @entryPointScripts('app')
</head>
<body>
    <div>dzqddzqzd</div>
    {{--  <ul>
    {{console.log(auth.user)}}
        @if(!auth.user)
            <li><a href="/login">Login</a></li>
        @else
            <li>Welcome {{auth.user.email}} <a href="/logout">Logout</a></li>
        @end
    </ul>  --}}
    @!section('body')
</body>
</html>